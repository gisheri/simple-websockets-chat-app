{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "immer": "^4.0.1",
    "randomcolor": "^0.5.4",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0",
    "react-socket-io": "^0.2.5",
    "react-websocket": "^2.1.0",
    "styled-components": "^4.4.0",
    "throttle-debounce": "^2.1.0",
    "typescript": "^3.6.3",
    "use-immer": "^0.3.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "aws s3 sync build/ s3://ericgish-sls-chat --acl public-read"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
